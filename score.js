let scores = [45, 80, 92, 38, 65];

// --- ช่วยนักเรียนใช้ .map() เพื่อบวกคะแนนจิตพิสัยเพิ่ม
let adjustedScores = scores.map(score => score + 5);

// --- หาคนตกใช้ .filter() เพื่อหาคะแนนที่น้อยกว่า 50
let failedScores = adjustedScores.filter(score => score < 50);

// --- สรุปผลสร้างฟังก์ชัน report เพื่อรับรายการคนตก แล้วแจ้งเตือนจำนวนคนตก
function report(failedList) {
    let count = failedList.length;
    console.log("--- สรุปผลการสอบ ---");
    console.log(`คะแนนทั้งหมดหลังบวกเพิ่ม: [${adjustedScores.join(", ")}]`);
    
    if (count > 0) {
        console.log(`⚠️ มีนักเรียนสอบตกจำนวน: ${count} คน`);
        console.log(`คะแนนของคนที่ไม่ผ่านคือ: ${failedList.join(", ")}`);
    } else {
        console.log("🎉 ยินดีด้วย! สอบผ่านทุกคน");
    }
}

// เรียกใช้งานฟังก์ชัน
report(failedScores);